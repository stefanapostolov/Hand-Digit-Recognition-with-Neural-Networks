# Hand-Digit-Recognition-with-Neural-Networks

This script uses a neural network using RELU activations and ADAM optimizing algorithm to train a model that recognizes a hand written digit from 0 to 9
